<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.app.wikipedia">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_wikipedia_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_wikipedia_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wikipedia"
        android:usesCleartextTraffic="true"
        tools:replace="android:icon"
        tools:targetApi="31">
        <activity
            android:name=".ui.activity.MainActivity4"
            android:exported="false" />
        <activity
            android:name=".ui.activity.MainActivity3"
            android:exported="false" />
        <activity
            android:name=".ui.activity.MainActivity2"
            android:exported="false" />
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>